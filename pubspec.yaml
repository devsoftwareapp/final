name: pdfreader
description: "PDF Reader"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_inappwebview: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    # Ana HTML dosyası (DIRECT assets/index.html)
    - assets/index.html
    
    # Ek asset'ler
    - assets/web/
    - assets/build/
    - assets/web/images/
    - assets/web/fonts/
    - assets/web/css/
    - assets/web/js/
